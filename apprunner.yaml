version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - cd api
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.8.5
  command: uvicorn main:app --host 0.0.0.0 -port 8080